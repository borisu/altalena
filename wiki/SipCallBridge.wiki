== `:makecalle(...)` ==
_ctor_
creates CnxInfoBridge object

|| *named parameter* || *description* ||
|| dest || destination ||
|| timeout || client side timeout ||

Makes call to the destination, the INVITE sent is empty the call should answered lately. 

{{{

}}}